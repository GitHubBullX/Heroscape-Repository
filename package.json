{
  "name": "project-valhalla",
  "version": "0.10.0",
  "private": true,
  "description": "Project Valhalla tactical hex battlefield",
  "engines": { "node": ">=22" },
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "start": "node scripts/serve.mjs",
    "test": "node --test",
    "check": "node --check dist/game.js && node scripts/check.mjs",
    "build": "node scripts/check.mjs"
  }
}
